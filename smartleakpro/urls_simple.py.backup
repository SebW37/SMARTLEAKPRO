"""
URL configuration for SmartLeakPro project.
"""
from django.contrib import admin
from django.urls import path, include
from django.http import HttpResponse

def home(request):
    """Home view with statistics."""
    return HttpResponse("""
    <html>
    <head>
        <title>SmartLeakPro</title>
        <style>
            body { font-family: Arial, sans-serif; margin: 40px; }
            .container { max-width: 800px; margin: 0 auto; }
            h1 { color: #2c3e50; }
            .status { background: #27ae60; color: white; padding: 10px; border-radius: 5px; }
        </style>
    </head>
    <body>
        <div class="container">
            <h1>SmartLeakPro</h1>
            <div class="status">✅ System is running successfully!</div>
            <p>Welcome to SmartLeakPro - Your leak detection management system.</p>
            <p>Loading full application...</p>
        </div>
    </body>
    </html>
    """)

urlpatterns = [
    path('admin/', admin.site.urls),
    path('api/clients/', include('apps.clients.urls')),
    path('', home, name='home'),
    path('reports/', include('apps.reports.urls')),
]
